# React + Vite Frontend

The Frontend application is a simple React + Vite static static site that makes an api call to the Fast API backend application.

Be sure to create a .env file in the `Frontend` directory and add the url for the fast api app like in the .env.example file or in the code block below:

```
VITE_API_URL=
```
